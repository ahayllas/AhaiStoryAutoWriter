You are planning ONE active section into a section-level execution blueprint.
This blueprint will guide downstream per-segment expansion.

Your goal is to prevent structural drift, repetition, and accidental re-planning of the same section.

Return JSON only.

# Inputs you will receive
You will only receive a carefully reduced context:
- story_overview: Core story information (protagonist, premise, conflict, tone, etc.)
- prev_section: The section before the current one (can be null)
- current_section: The section you must plan right now (this is the focus)
- next_section: The section after the current one (can be null)
- current_story_state
- existing_section_plan (if any)
- metric_schema

You should NOT assume knowledge of any other sections unless they are explicitly provided in the inputs above.

# Output goals
Produce a section plan for exactly ONE active section following the required JSON schema.

The plan must:
1. Cover only the current active section.
2. Decompose the section into distinct segment blueprints.
3. Assign mandatory events across the segments deliberately.
4. Define what each segment must accomplish and avoid.
5. define what each segment must avoid repeating,
6. remain compatible with the current story state,
7. adapt if the section is already partially completed.

# Required JSON schema

{
  "section_id": "string",
  "title": "string",
  "planning_basis": {
    "purpose": "string",
    "summary": "string",
    "desired_end_state": "string",
    "ending_trigger": "string",
    "next_section_handoff": "string",
    "is_final_section": true
  },
  "progress_assessment": {
    "already_completed_segment_count": 0,
    "already_hit_beats": [
      "string"
    ],
    "remaining_mandatory_events": [
      "string"
    ],
    "planning_mode": "fresh|adaptive"
  },
  "section_strategy": {
    "entry_state": "string",
    "core_progression": [
      "string"
    ],
    "global_avoid": [
      "string"
    ]
  },
  "segment_blueprints": [
    {
      "segment_index": 1,
      "section_phase": "opening|middle|closing",
      "status": "planned|locked_done|needs_adaptation",
      "segment_role": "string",
      "segment_objective": "string",
      "primary_beat": "string",
      "secondary_beats": [
        "string"
      ],
      "mandatory_events_assigned": [
        "string"
      ],
      "must_include": [
        "string"
      ],
      "must_not_repeat": [
        "string"
      ],
      "must_not_resolve_yet": [
        "string"
      ],
      "expected_end_state": "string",
      "handoff_to_next_segment": "string"
    }
  ]
}

# Planning rules

- The number of segments must be between planned_segments_min and planned_segments_max if available.
- If the section already has segment_count > 0, treat this as an adaptive planning problem:
  - preserve what is already done,
  - do not re-plan completed work,
  - adjust the remaining blueprints only.
- Each segment must have a distinct job.
- "segment_objective" must be concrete and achievable in one segment.
- "primary_beat" must be specific and observable.
- "mandatory_events_assigned" should distribute remaining mandatory events deliberately instead of repeating them everywhere.
- "must_not_repeat" should explicitly block likely redundancy from earlier or adjacent segments.
- "must_not_resolve_yet" should protect pacing and prevent premature payoff.
- "expected_end_state" must describe a changed state, not just mood.

# Anti-failure rules
- Do not produce multiple blueprints that all say versions of "conflict escalates".
- Do not assign the same mandatory event to every segment.
- Do not make closing beats appear in the opening unless the section is only one segment.
- Do not leave the section without a credible path to desired_end_state.
- Do not produce vague labels instead of executable structural guidance.


story overview:
```
{{STORY_OVERVIEW_JSON}}
```

previous section:
```
{{PREV_SECTION_JSON}}
```
current section:
```
{{CURRENT_SECTION_JSON}}
```

Next section:
```
{{NEXT_SECTION_JSON}}
```

current story state:
```
{{CURRENT_STORY_STATE_JSON}}
```

existing section plan:
```
{{EXISTING_SECTION_PLAN_JSON}}
```

metric schema:
```
{{METRIC_SCHEMA_JSON}}
```

Plan the "current section" only.
Return JSON only.